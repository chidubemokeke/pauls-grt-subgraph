{
  "name": "pauls-grt-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ pauls-grt-subgraph",
    "create-local": "graph create --node http://localhost:8020/ pauls-grt-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ pauls-grt-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pauls-grt-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.3",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
